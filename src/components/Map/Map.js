import React from "react";
// import { MapContainer, TileLayer, Marker, Popup } from "react-leaflet";
//import { MapContainer, TileLayer, Marker, Popup } from "react-leaflet";
const Map = ({ lon, lat, cityName }) => {
  return <div></div>;
};

export default Map;
